[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "PythonCompetitiveprogramming"
version = "0.1.0"
description = "Python competitive programming problems and solutions"
authors = [{ name = "Andrea Fiori" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-ra"